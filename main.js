var firstA = [];
firstA.sort();

var secondA = [];
secondA.sort(function(){
  
  for (var i = 0; i < secondA.length; i++) {
  return secondA[i] > 25;
};

var button = $('input').click(function(){
  return secondA();
  console.log(button);
});

var strInput = [];
var newStrInput = strInput.split(',');
  for (var j=0; i<newStrInput.length; j++);
    newStrInput[j]=parseInt(newStrInput[i]);
  console.log(newStrInput);
});

console.log(secondA);


